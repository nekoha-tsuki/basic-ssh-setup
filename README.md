# basic-ssh-setup
A bash script for quick and secure ssh setup.

## Installation
Getting started is easy. Just run the command as root:
```bash
bash <(curl -sSL https://raw.githubusercontent.com/nekoha-tsuki/basic-ssh-setup/main/setup.sh)
```
